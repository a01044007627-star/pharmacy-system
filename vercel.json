{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci --include=dev --no-audit --no-fund",
  "framework": "nextjs",
  "regions": [
    "iad1"
  ],
  "functions": {
    "src/app/**/*.ts": {
      "maxDuration": 30
    }
  }
}
